{
  "scripts": {
    "build": "npx tailwindcss -i ./src/input.css -o ./docs/output.css --minify && cp ./src/*.html ./docs/ && mkdir -p ./docs/fonts && cp ./fonts/* ./docs/fonts/"
  },
  "devDependencies": {
    "tailwindcss": "^3.0.24"
  }
}
